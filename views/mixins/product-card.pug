mixin product-card(product)
	.col-md-3
		article.card
			img.card-img-top(
				src= product.image
				alt= `Изображение ${product.name}`
			)
			.card-body
				h5.card-title= product.name
				p.card-text= `Стоимость: ${product.price}`
				a.btn.btn-primary(href="#") Посмотреть товар