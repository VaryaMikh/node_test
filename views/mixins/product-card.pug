mixin product-card(product)
	.col-md-3.mb-3
		article.card
			img.card-img-top(
				src= product.image 
				alt=`Изображение ${product.name}`
			)
			.card-body
				h5.card-title= product.name
				p.card-text
					span Цена: #{product.price - product.discount}p
					if product.discount != 0
						del.small.text-mutted.text-secondary.ml-2 #{product.price}₽
				a.btn.btn-primary(href="#") Посмотреть товар
